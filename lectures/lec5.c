/* Lecture

	files contain bytes

	a link is a pointer


	a directory is a list of pairs: (name, i-number)
	a name is a string up to 255 characters
		// opening a file is very expensive!!
	
	world wide web is all the web pages and the hyperlinks between them
	the internet is the physical network of computers

	a hard link is a normal directory of the form: name i-number


	% ln foo fool
	'fool' is a hard link to file 'foo'
	% rm fool
	rm removes the hard link fool
	removing the last link to a file frees the file space
*/

